# LiveSteelProof Font License

`assets/LiveSteelProof.woff2` is built from Roboto Black provided by the npm
package `@fontsource/roboto@5.2.10`.

- Package license: OFL-1.1
- Package source: `@fontsource/roboto`
- Release package role: deterministic proof rasterization font

The release build is fail-closed: it requires the bundled font asset and will not
silently substitute a host system font.
